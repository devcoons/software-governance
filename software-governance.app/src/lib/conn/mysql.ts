import {
  DB_HOST, DB_PORT, DB_USER, DB_PASS, DB_NAME,
} from '@/auth.config';

import mysql, { Pool } from 'mysql2/promise';

/* ------------------------------------------------------- */
/* ------------------------------------------------------- */
/* ------------------------------------------------------- */
